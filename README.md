# EmoSnap Website repository

ここは Website / LP 用の空ディレクトリです。
将来的に `Next.js` 等のフロントエンドプロジェクトをこのディレクトリに配置してください。

次のステップ例:

1. GitHub 上で `emosnap/website` リポジトリを作成（private 推奨）
2. このローカル `emosnap/website` を新リポジトリに push
3. Vercel で新リポジトリを連携して Preview を有効化

必要なら私が `Next.js` の最小テンプレをここに作成します。